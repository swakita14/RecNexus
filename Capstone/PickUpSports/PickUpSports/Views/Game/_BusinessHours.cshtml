@model List<PickUpSports.Models.ViewModel.BusinessHoursViewModel>

<div class="well" style="padding-bottom: 20px">
    <div class="row">
        <h6>
            <b>Hours of Operation:</b>
        </h6>
    </div>
    @if (Model.Count > 0)
    {
        foreach (var businessHour in Model)
        {
            <div class="row">
                @Html.DisplayFor(x => businessHour.DayOfWeek): @Html.DisplayFor(x => businessHour.OpenTime) - @Html.DisplayFor(x => businessHour.CloseTime)
            </div>
        }
    }
    else
    {
        <div class="row">
            We apologize. Business hours are not currently available for this venue.
        </div>
    }
</div>